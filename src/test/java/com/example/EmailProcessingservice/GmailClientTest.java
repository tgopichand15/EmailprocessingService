package com.example.EmailProcessingservice;

import org.junit.jupiter.api.Test;

import java.util.ArrayList;

public class GmailClientTest {

    //preparing data
    ArrayList<String> i=new ArrayList<String>();
    i.add("10.20.30.45,CPU,CPU crossed 80%,85);

    when(r.check().thenReturn(l);

    @Test
    public void checkmails(){

        GmailClient g=new GmailClient();
        ArrayList<String> output=g.check();

        AssertTrue(g.contains("10.20.30.45,CPU,CPU crossed 80%,85);
    }
}
