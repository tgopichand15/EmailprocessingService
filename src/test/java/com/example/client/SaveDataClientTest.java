/*package com.example.client;


import org.junit.Test;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.util.HashMap;

public class SaveDataClientTest {

    @Test
    public void test(){
        SaveDataClient s=new SaveDataClient();

        HistoricalIssue h=new HistoricalIssue(1,"CPU","10.20.30.245",2);

        when(s.createNewIssue(h).thenreturn(null));

        assertEqual(s.createNewIssue(h),null);

    }

    @Test
    public void test1(){
        SaveDataClient s=new SaveDataClient();

        HistoricalIssue h=new HistoricalIssue(1,"CPU","10.20.30.245",2);

        when(s.createHistoricalIssue(h).thenreturn(null);

        assertEqual(s.createHistoricalIssue(h),null);

    }

    @Test
    public void test2(){

        SaveDataClient s=new SaveDataClient();

        UnresolvedIssue h=new UnresolvedIssue(1,"Wintel","10.20.30.245",2);

        when(s.createHistoricalIssue(h).thenreturn(10);

        assertEqual(s.insertUnresovedIsseus(h),10);

    }

    @Test
    public void Test3(){

        SaveDataClient s=new SaveDataClient();

        Issue i=new Issue(1,"CPU","10.20.30.245",false);

        HashMap<String,String> hm=new HashMap<String,String>();
        hm.add("CPU","10.20.30.245");

        when(s.createIssueforProcessingintoDB(h).thenreturn(i);

        Assertions.assertEquals(s.createIssueforProcessingintoDB(h),i);

    }

    @Test
    public void Test4(){

        SaveDataClient s=new SaveDataClient();

        Issue i=new Issue(1,"CPU","10.20.30.245",false);

        HashMap<String,String> hm=new HashMap<String,String>();
        hm.add("CPU","10.20.30.245");

        when(s.insertIssuestobeProcessedBysolutionService(h).thenreturn(i);

        Assertions.assertEquals(s.insertIssuestobeProcessedBysolutionService(h),i);

        insertIssuestobeProcessedBysolutionService
    }









}*/
